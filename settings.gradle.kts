pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "Veterinarian app"
include(":androidApp")
include(":shared")
include(":petCore")
